# 更新日志（CHANGELOG）

## v3.4（2026-09-21）—— 补断言测试(43 条) + 修两处硬编码版本号

- 此前【零测试】(2125 行)。新增 `test_starry_sky.js`, **43 条断言**。
- **修版本号漏洞**: 窗口标题此前**硬编码 `v3.2`**、启动日志也硬编码 ——
  而 `VER` 常量早已是 3.3。窗口标题已改为引用 `+ VER`; 启动日志那行在**裸块之外**,
  访问不到 `buildUI` 内的 `VER`, 只能硬编码, 由测试的"启动日志版本号 == VER"断言守。
  文件头版本号同步对齐。
- ⚠️ **取函数方式与其他面板不同**: 本文件外壳是**裸块 `{ … }`** 而非 IIFE ——
  块内 `return` 不合法, 标准导出闸门写不进去, 故改用**文本提取**(按花括号配对切函数体)。
  零源码改动也能测; 将来若改成 IIFE, 建议换回标准闸门。
- 反向对照: 注入 4 处 → **8 条变红**。过程中发现一条**弱断言** ——
  把 `Math.round` 改成 `Math.floor` 时, 原先所有颜色断言**照样通过**
  (纯色用例两条路径结果相同), 已补"半通道值四舍五入"(30° / 210°)专门抓它。

## v3.3（2026-09-21）—— 面板顶部显示版本号

- 新增 `VER` 常量（版本号单一真相：文件头注释 / 常量 / CHANGELOG 顶部三处一致）
- 面板顶部常驻显示 `星空粒子生成器  v3.3`
- 为什么必须：ScriptUI Panel 由 **AE 启动时**载入 —— 改完脚本不重启 AE，面板里跑的还是旧版，
  AE 报的错也可能是旧文件的行号。没有版本号就无法判断 AE 里跑的是不是最新的。

## v3.2

- 吸引逻辑重写（速度加法公式，力度与吸引时长解耦）
- 密度参数实现（seedRandom 随机决定遮罩内/全合成发射）
- 多处硬伤修复（预设中文 Key 失效、下拉 selection 管理、表达式死代码）
- UI 布局统一

## v3.1.2

- Mask 羽化模糊（规避 Effect → Mask 渲染顺序陷阱）
- 启动性能优化（槽位懒加载）
- 中文注释 + UI 布局自适应

## v2.0

- 发射区域（全合成/遮罩范围/密度）
- 目标吸引 + 形状选择

## v1.4

- Solid 粒子方案（初版）
